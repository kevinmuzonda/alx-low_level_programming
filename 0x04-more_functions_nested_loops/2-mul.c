#include "main.h"

/**
  * mul - A function that multiples integers.
  * @a:is the first intgers.
  * @b: is the second intger.
  * Return: Value of a *  b.
 */
int mul(int a, int b)
{
	return (a * b);
}
