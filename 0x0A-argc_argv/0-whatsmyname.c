#include <stdio.h>
#include <stdlib.h>

/**
  * main - Entry point
  * @argc: takes integer values
  * @argv: takes in strings
  * Return: always (success).
 */

int main(int argc, char* argv[])
{
	argc = argc;

	printf("%s\n",*argv);
	return (0);
}
